<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.inceptai.dobby">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/wifi_expert_launcher"
        android:label="@string/wifiexpert_app_name"
        android:roundIcon="@mipmap/wifi_expert_launcher"
        android:supportsRtl="true"
        android:name=".DobbyApplication"
        android:theme="@style/AppTheme"
        tools:replace="android:theme, android:label, android:roundIcon, android:icon">
        <activity
            android:name=".MainActivity"
            android:label="@string/wifiexpert_app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
